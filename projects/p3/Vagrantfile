# Automatically Generated by Vagrant Config Generator, see https://github.com/jianan1104/vagrantfile-generator
Vagrant.configure('2') do |config|
  (1..1).each do |i|
  config.vm.define "ans" do |machine|
    machine.vm.box = 'generic/ubuntu2204'
    machine.vm.hostname = "ans"
    machine.vm.provider "hyperv" do |vb|
      vb.vmname = "ansible-#{i}"
      vb.cpus = '1'
      vb.memory = '2048'
    end
  end
  end
end
